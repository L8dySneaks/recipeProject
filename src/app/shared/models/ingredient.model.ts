export class Ingredient {
  // public name: string;
  // public amount: number;
  constructor(public name: string, public amount: number) {
    // this.name = name;
    // this.amount = amount;
  }
}

// The commented out portion is the long hand version. You can 
// comment out the following and use the TypeScript short cut for creating a model.